# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.moved-aside
*.xcuserstate
*.xccheckout
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# XcodeGen output — the project is generated, not committed.
# Run `xcodegen generate` after cloning or changing project.yml.
# The UIWorkouts version is pinned in project.yml (exactVersion), so no
# Package.resolved is needed under version control.
Generated-Info.plist
Rounds.entitlements
/Rounds.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (if ever added)
Pods/
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# macOS
.DS_Store

# Simulator / test artifacts
*.gcno
*.gcda
*.xcresult
